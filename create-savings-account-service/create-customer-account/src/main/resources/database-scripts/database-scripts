ALTER SESSION SET CURRENT_SCHEMA = NGA;

CREATE TABLE CUSTOMER_ACCOUNT (
    CUSTOMER_ID NUMBER,
    CUSTOMER_NAME varchar(255),
    ACCOUNT_NUMBER varchar(255),
    ACCOUNT_TYPE varchar(255),
    CREATED_ON DATE
);

create sequence CUSTOMER_ACCOUNT_SEQUENCE start with 8000;