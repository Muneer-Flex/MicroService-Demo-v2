ALTER SESSION SET CURRENT_SCHEMA = NGA;

CREATE TABLE CUSTOMER_PROFILE (
    CUSTOMER_ID NUMBER,
    CUSTOMER_NAME varchar(255),
    CUSTOMER_EMAIL varchar(255),
    GOVERNING_STATE varchar(255),
    CREATED_ON DATE
);

create sequence CUSTOMER_PROFILE_SEQUENCE start with 3000;